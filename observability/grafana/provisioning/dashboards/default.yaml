apiVersion: 1
providers:
  - name: MCP telemetry
    orgId: 1
    folder: MCP Gateway
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
